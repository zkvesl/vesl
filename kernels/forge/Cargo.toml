[package]
name = "kernels-forge"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Vesl Forge kernel JAM — generic STARK prover (~18MB)"

[dependencies]
sha2 = "0.10"

[build-dependencies]
sha2 = "0.10"
