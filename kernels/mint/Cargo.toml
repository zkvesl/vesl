[package]
name = "kernels-mint"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Vesl Mint kernel JAM — commitment only (1.5MB)"

[dependencies]
sha2 = "0.10"

[build-dependencies]
sha2 = "0.10"
